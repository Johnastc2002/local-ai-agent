# Qwen3.6-27B @ ~524k context (YaRN factor 2.0) — target: 1× H200 SXM (141 GB)
# Pod: MODEL_PROFILE=production-500k bash scripts/install-on-pod.sh

MODEL_NAME=Qwen/Qwen3.6-27B-FP8
MAX_MODEL_LEN=524288
GPU_MEMORY_UTILIZATION=0.92
TOOL_CALL_PARSER=qwen3_xml
ENABLE_AUTO_TOOL_CHOICE=true
REASONING_PARSER=qwen3

# vLLM long-context (see production-500k-yarn.json)
VLLM_KV_CACHE_DTYPE=fp8
VLLM_LANGUAGE_MODEL_ONLY=true
VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
VLLM_TENSOR_PARALLEL_SIZE=1

# ICR: 2 outer iterations, full critique loop each time
CURSOR_PLAN_MAX_ITERATIONS=2
REFINE_MAX_TOKENS=8192
REFINE_MAX_TOKENS_CAP=8192
REFINE_MEMORY_EVERY=10

# Gateway trim / ICR (large window — trim only when actually over budget)
SYSTEM_PREFIX_MAX_TOKENS=16384
MIN_TAIL_MESSAGES=64
ICR_MAX_CHARS=16000
